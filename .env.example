# Required only for the AI chat assistant; boards and accounts work without it.
OPENROUTER_API_KEY=

# Optional. Signs session cookies. When unset, a random secret is generated on
# every start, which signs everyone out whenever the server restarts.
SESSION_SECRET=
